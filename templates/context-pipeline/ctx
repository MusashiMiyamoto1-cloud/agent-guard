#!/bin/bash
# Context Pipeline CLI
# Usage: ./ctx "query" [--compress] [-k N]

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$DIR"

# Activate venv if exists
if [ -d ".venv" ]; then
    source .venv/bin/activate
fi

python pipeline.py "$@"
